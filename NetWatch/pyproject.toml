[project]
name = "netwatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "flask>=3.1.3",
    "python-whois>=0.9.6",
    "rich>=14.3.3",
]
